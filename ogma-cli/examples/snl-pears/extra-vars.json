{ "copilot_extra_defs":
    [ "#define if if_"
    , "#define when when_"
    , "#define while while_"
    , "#define during while_"
    , "#define where where_"
    , "#define is =="
    , "#define shall"
    , "#define satisfy"
    , "#define the"
    , "#define then"
    , "#define system"
    , ""
    , "while_ :: Stream Bool -> Stream Bool -> Stream Bool"
    , "while_ = (==>)"
    , ""
    , "when_ :: Stream Bool -> Stream Bool -> Stream Bool"
    , "when_ = \\x y -> previous (risingEdge x) ==> y"
    , ""
    , "if_ :: Stream Bool -> Stream Bool -> Stream Bool"
    , "if_ = \\x y -> previous (risingEdge x) ==> y"
    , ""
    , "where_ :: Stream Bool -> Stream Bool -> Stream Bool"
    , "where_ = (==>)"
    , ""
    , "(&) = flip ($)"
    , ""
    , "risingEdge :: Stream Bool -> Stream Bool"
    , "risingEdge s = let previousS = [True] ++ s"
    , "               in s && not previousS"
    , ""
    , "engaged :: Stream Int32"
    , "engaged = 0"
    , ""
    ]
}
